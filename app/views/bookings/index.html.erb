<div class="tabs-container">
  <div class="tab-menu">
    <div class="tabs">
      <input type="radio" name="myradio" checked>
        <label>🚀<span class="label-lg">Incoming rides⭐</span></label>
      </input>
      <div class="tab-content">
        <% @my_own_bookings.each do |booking| %>
        <%# <%= link_to 'booking', do %>
          <%= render 'shared/bookingcard', booking: booking %>
        <%# <% end %>
        <% end %>

      </div>
    </div>
    <div class="tabs">
      <input type="radio" name="myradio">
        <label> 👾 <span class="label-lg">Space Moulah 👽</span></label>
      </input>
      <div class="tab-content">
        <% @my_ships_bookings.each do |booking| %>
          <%# <%= link_to 'booking', do %>
            <%= render 'shared/bookingcard', booking: booking %>
          <%# <% end %>
        <% end %>
      </div>
    </div>
    <div class="tabs">
      <input type="radio" name="myradio">
        <label> ⏰ <span class="label-lg">Good ol' stuff 📑</span></label>
      </input>
      <div class="tab-content">
        <% @all_my_past_bookings.each do |booking| %>
          <%# <%= link_to 'booking', do %>
            <%= render 'shared/bookingcard', booking: booking %>
          <%# <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
